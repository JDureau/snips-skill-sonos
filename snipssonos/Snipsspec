package_name: snipssonos
class_name: SnipsSonos
description: Sonos snips.skill for Snips
notifications:
  - name: start_listening
    action: |
      {%
      snips.skill.set_to_low_volume()
      %}
intents:
  - intent: radioOn
    action: |
      {%
      if intent.radio_name != None:
        snips.skill.turn_on_radio(intent.radio_name)
      snips.skill.set_to_previous_volume()
      %}
  - intent: speakerInterrupt
    action: "pause_sonos"
  - intent: volumeUp
    action: |
      {% snips.skill.set_to_previous_volume(); snips.skill.volume_up(intent.volume_higher) %}
  - intent: volumeDown
    action: |
      {% snips.skill.set_to_previous_volume(); snips.skill.volume_down(intent.volume_lower) %}
  - intent: playPlaylist
    action: |
      {%
      snips.skill.play_playlist(intent.playlist_name, _shuffle=(intent.playlist_lecture_mode is not None and intent.playlist_lecture_mode == "shuffle")); snips.skill.set_to_previous_volume()
      %}
  - intent: playArtist
    action: |
      {% snips.skill.play_artist(intent.artist_name); snips.skill.set_to_previous_volume() %}
  - intent: playSong
    action: |
      {% snips.skill.play_song(intent.song_name); snips.skill.set_to_previous_volume() %}
  - intent: playAlbum
    action: |
      {% snips.skill.play_album(intent.album_name, _shuffle=(intent.album_lecture_mode is not None and intent.album_lecture_mode == "shuffle")); snips.skill.set_to_previous_volume() %}
  - intent: resumeMusic
    action: |
      {% snips.skill.play(); snips.skill.set_to_previous_volume() %}
  - intent: nextSong
    action: |
      {% snips.skill.play_next_item_in_queue(); snips.skill.set_to_previous_volume() %}
  - intent: previousSong
    action: |
      {% snips.skill.play_previous_item_in_queue(); snips.skill.set_to_previous_volume() %}
  - intent: addSong
    action: |
      {% snips.skill.add_song(); snips.skill.set_to_previous_volume() %}
  - intent: getInfos
    action: |
      {%
        snips.skill.set_to_previous_volume(); tts_service.speak("This is {} by {} on the album {}".format(*snips.skill.get_info()))
      %}
  - intent: "*"
    action: |
      {%
        snips.skill.set_to_previous_volume();
      %}
